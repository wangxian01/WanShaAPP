<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.l.wanshaapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mianlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <activity android:name=".HomeBannerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity.SearchApp"/>
        <activity android:name=".Activity.SearchDealActivity"/>
        <activity android:name=".WanShaLogin.LoginActivity">
        </activity>
        <activity android:name=".DynamicHome.DynamicMainActivity"></activity>
        <activity android:name=".DynamicCommentDetails.CommentDetailsActivity"
                   android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".JCVideoPlayerStandard.player"></activity>
        <activity android:name=".WanShaLogin.RegisteredActivity"></activity>
        <activity android:name=".XiangQingActivity"></activity>
        <activity android:name=".Activity.yuyuexiazai"></activity>
        <activity android:name=".Activity.GuanzhuGameActivity"></activity>
        <activity android:name=".AttentionActivity"></activity>
        <activity android:name=".DyFocusCommentDetails.FcousCommentActivity"></activity>
        <activity android:name=".Activity.MyShouCangActivity"/>
        <activity android:name=".Activity.DownloadActivity"/>
    </application>

    <!-- ======================授权访问网络（OkHttpUtil）========================== -->
    <!-- 允许程序打开网络套接字 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 允许程序访问有关GSM网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许程序访问Wi-Fi网络状态信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 允许一个程序访问精良位置(如GPS)  -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 访问电话状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 允许程序写入外部存储，如SD卡上写文件 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 允许程序读外部存储，如SD卡上读文件 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!-- ======================授权访问网络（HttpUtil）========================== -->
    <!-- 允许程序打开网络套接字 -->
    <!--<uses-permission android:name="android.permission.INTERNET"/>-->
    <!-- 允许程序访问有关GSM网络信息 -->
    <!--<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />-->
    <!-- 允许程序访问Wi-Fi网络状态信息 -->
    <!--<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />-->
    <!-- 允许一个程序访问精良位置(如GPS)  -->
    <!--<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->
    <!-- 访问电话状态 -->
    <!--<uses-permission android:name="android.permission.READ_PHONE_STATE" />-->
</manifest>